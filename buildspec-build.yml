version: 0.2

env:
  variables:
    ENVIRONMENT: "dev"
    REGION: "us-east-1"

phases:
  build:
    commands:
      - bash scripts/pipeline-build.sh

  post_build:
    commands:
      - echo "Build phase complete"
